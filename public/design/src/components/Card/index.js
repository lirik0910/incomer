import Base from '../../Base.js';

export default class Card extends Base {
	initDOMElements(e) {
        this.els = {
        	// _headerNav: $('.header__nav')
        }
    }

	onDOMReady(e) {
		
	}
}
