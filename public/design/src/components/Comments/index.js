import Base from '../../Base.js';

export default class Comments extends Base {
	initDOMElements(e) {
        this.els = {
        	// _headerNav: $('.header__nav'),
        }
    }

	onDOMReady(e) {

	}
}
