import Base from '../../Base.js';

export default class CompanyNews extends Base {
	initDOMElements(e) {
        this.els = {
        	// _infoTabs: $('.info__tabs'),
        }
    }

	onDOMReady(e) {
		
	}
}
