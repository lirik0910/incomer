import Base from '../../Base.js';

export default class Tags extends Base {
	initDOMElements(e) {
        this.els = {
        	// _headerNav: $('.header__nav')
        }
    }

	onDOMReady(e) {
		
	}
}
